{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Allows the main BaudTide window to receive native serial-session events.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
